{
  "name": "marc-frbr.js",
  "version": "0.2.0",
  "description": "FRBR style clustering for MARC cataloging records.",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "npm test && node --experimental-strip-types build.ts",
    "test": "node --import tsx --test tests/**/*.test.ts"
  },
  "keywords": [
    "library",
    "cataloging",
    "marc",
    "bibliographic description",
    "cluster",
    "frbr"
  ],
  "author": "Steve Meyer",
  "license": "MIT",
  "devDependencies": {
    "@tsconfig/node24": "^24.0.1",
    "@types/node": "^24.2.1",
    "esbuild": "^0.25.8",
    "tsx": "^4.20.3",
    "typescript": "^5.9.2"
  }
}
